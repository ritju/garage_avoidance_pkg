#include "garage_utils/shortest_path_search.hpp"

double points_list[10][2] =
{
        {},
} ;

int main()
{
        // rclcpp::init(argc, argv);
        // auto node = std::make_shared<rclcpp::Node>("test_shortest_path_search");

        // std::vector<garage_utils_pkg::EnhancedPoint> points;

        // auto path_searcher = new garage_utils_pkg::ShortestPathSearch();
        
        // auto path = path_searcher->get_path();

        // std::stringstream ss;
        // for (size_t i = 0; i < path.size(); i++)
        // {
        //         ss << path[i] << " ";
        // }
        // RCLCPP_INFO(node->get_logger(), "path => %s", ss.str().c_str());


        // rclcpp::shutdown();
        printf("1");
}




